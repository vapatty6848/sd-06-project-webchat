<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>History</title>
</head>
<body>
  <ul class="list-group" id="chat-body">
    <% messages.forEach((message) => { %>
      <li data-testid='message'><%=`${helpers.dateFormater(message.timestamp)} ${message.nickname} - ${message.message}`%></li>
    <% }) %>
  </ul>
</body>
</html>